@page
@model Proiect_web_Frizerie.Pages.Appointments.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Appointment</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Appointment.ClientID" class="control-label">Client</label>
                <select asp-for="Appointment.ClientID" class ="form-control" asp-items="ViewBag.ClientID" ></select>
            </div>
            <div class="form-group">
                <label asp-for="Appointment.StylistID" class="control-label">Stilist</label>
                <select asp-for="Appointment.StylistID" class ="form-control" asp-items="ViewBag.StylistID" ></select>
            </div>
            <div class="form-group">
                <label asp-for="Appointment.ServiceID" class="control-label">Serviciu</label>
                <select asp-for="Appointment.ServiceID" class ="form-control" asp-items="ViewBag.ServiceID" ></select>
            </div>
            <div class="form-group">
                <label asp-for="Appointment.DataOra" class="control-label">Data si Ora</label>
                <input asp-for="Appointment.DataOra" class="form-control" />
                <span asp-validation-for="Appointment.DataOra" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
